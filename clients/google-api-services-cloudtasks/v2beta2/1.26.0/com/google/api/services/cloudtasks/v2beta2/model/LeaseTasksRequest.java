/*
 * Licensed under the Apache License, Version 2.0 (the "License"); you may not use this file except
 * in compliance with the License. You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software distributed under the License
 * is distributed on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express
 * or implied. See the License for the specific language governing permissions and limitations under
 * the License.
 */
/*
 * This code was generated by https://github.com/googleapis/google-api-java-client-services/
 * Modify at your own risk.
 */

package com.google.api.services.cloudtasks.v2beta2.model;

/**
 * Request message for leasing tasks using LeaseTasks.
 *
 * <p> This is the Java data model class that specifies how to parse/serialize into the JSON that is
 * transmitted over HTTP when working with the Cloud Tasks API. For a detailed explanation see:
 * <a href="https://developers.google.com/api-client-library/java/google-http-java-client/json">https://developers.google.com/api-client-library/java/google-http-java-client/json</a>
 * </p>
 *
 * @author Google, Inc.
 */
@SuppressWarnings("javadoc")
public final class LeaseTasksRequest extends com.google.api.client.json.GenericJson {

  /**
   * `filter` can be used to specify a subset of tasks to lease.
   *
   * When `filter` is set to `tag=` then the response will contain only tasks whose tag is equal to
   * ``. `` must be less than 500 characters.
   *
   * When `filter` is set to `tag_function=oldest_tag()`, only tasks which have the same tag as the
   * task with the oldest schedule_time will be returned.
   *
   * Grammar Syntax:
   *
   * * `filter = "tag=" tag | "tag_function=" function`
   *
   * * `tag = string`
   *
   * * `function = "oldest_tag()"`
   *
   * The `oldest_tag()` function returns tasks which have the same tag as the oldest task (ordered
   * by schedule time).
   *
   * SDK compatibility: Although the SDK allows tags to be either string or [bytes](https://cloud.go
   * ogle.com/appengine/docs/standard/java/javadoc/com/google/appengine/api/taskqueue/TaskOptions.ht
   * ml#tag-byte:A-), only UTF-8 encoded tags can be used in Cloud Tasks. Tag which aren't UTF-8
   * encoded can't be used in the filter and the task's tag will be displayed as empty in Cloud
   * Tasks.
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private java.lang.String filter;

  /**
   * After the worker has successfully finished the work associated with the task, the worker must
   * call via AcknowledgeTask before the schedule_time. Otherwise the task will be returned to a
   * later LeaseTasks call so that another worker can retry it.
   *
   * The maximum lease duration is 1 week. `lease_duration` will be truncated to the nearest second.
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private String leaseDuration;

  /**
   * The maximum number of tasks to lease.
   *
   * The system will make a best effort to return as close to as `max_tasks` as possible.
   *
   * The largest that `max_tasks` can be is 1000.
   *
   * The maximum total size of a lease tasks response is 32 MB. If the sum of all task sizes
   * requested reaches this limit, fewer tasks than requested are returned.
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private java.lang.Integer maxTasks;

  /**
   * The response_view specifies which subset of the Task will be returned.
   *
   * By default response_view is BASIC; not all information is retrieved by default because some
   * data, such as payloads, might be desirable to return only when needed because of its large size
   * or because of the sensitivity of data that it contains.
   *
   * Authorization for FULL requires `cloudtasks.tasks.fullView` [Google
   * IAM](https://cloud.google.com/iam/) permission on the Task resource.
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private java.lang.String responseView;

  /**
   * `filter` can be used to specify a subset of tasks to lease.
   *
   * When `filter` is set to `tag=` then the response will contain only tasks whose tag is equal to
   * ``. `` must be less than 500 characters.
   *
   * When `filter` is set to `tag_function=oldest_tag()`, only tasks which have the same tag as the
   * task with the oldest schedule_time will be returned.
   *
   * Grammar Syntax:
   *
   * * `filter = "tag=" tag | "tag_function=" function`
   *
   * * `tag = string`
   *
   * * `function = "oldest_tag()"`
   *
   * The `oldest_tag()` function returns tasks which have the same tag as the oldest task (ordered
   * by schedule time).
   *
   * SDK compatibility: Although the SDK allows tags to be either string or [bytes](https://cloud.go
   * ogle.com/appengine/docs/standard/java/javadoc/com/google/appengine/api/taskqueue/TaskOptions.ht
   * ml#tag-byte:A-), only UTF-8 encoded tags can be used in Cloud Tasks. Tag which aren't UTF-8
   * encoded can't be used in the filter and the task's tag will be displayed as empty in Cloud
   * Tasks.
   * @return value or {@code null} for none
   */
  public java.lang.String getFilter() {
    return filter;
  }

  /**
   * `filter` can be used to specify a subset of tasks to lease.
   *
   * When `filter` is set to `tag=` then the response will contain only tasks whose tag is equal to
   * ``. `` must be less than 500 characters.
   *
   * When `filter` is set to `tag_function=oldest_tag()`, only tasks which have the same tag as the
   * task with the oldest schedule_time will be returned.
   *
   * Grammar Syntax:
   *
   * * `filter = "tag=" tag | "tag_function=" function`
   *
   * * `tag = string`
   *
   * * `function = "oldest_tag()"`
   *
   * The `oldest_tag()` function returns tasks which have the same tag as the oldest task (ordered
   * by schedule time).
   *
   * SDK compatibility: Although the SDK allows tags to be either string or [bytes](https://cloud.go
   * ogle.com/appengine/docs/standard/java/javadoc/com/google/appengine/api/taskqueue/TaskOptions.ht
   * ml#tag-byte:A-), only UTF-8 encoded tags can be used in Cloud Tasks. Tag which aren't UTF-8
   * encoded can't be used in the filter and the task's tag will be displayed as empty in Cloud
   * Tasks.
   * @param filter filter or {@code null} for none
   */
  public LeaseTasksRequest setFilter(java.lang.String filter) {
    this.filter = filter;
    return this;
  }

  /**
   * After the worker has successfully finished the work associated with the task, the worker must
   * call via AcknowledgeTask before the schedule_time. Otherwise the task will be returned to a
   * later LeaseTasks call so that another worker can retry it.
   *
   * The maximum lease duration is 1 week. `lease_duration` will be truncated to the nearest second.
   * @return value or {@code null} for none
   */
  public String getLeaseDuration() {
    return leaseDuration;
  }

  /**
   * After the worker has successfully finished the work associated with the task, the worker must
   * call via AcknowledgeTask before the schedule_time. Otherwise the task will be returned to a
   * later LeaseTasks call so that another worker can retry it.
   *
   * The maximum lease duration is 1 week. `lease_duration` will be truncated to the nearest second.
   * @param leaseDuration leaseDuration or {@code null} for none
   */
  public LeaseTasksRequest setLeaseDuration(String leaseDuration) {
    this.leaseDuration = leaseDuration;
    return this;
  }

  /**
   * The maximum number of tasks to lease.
   *
   * The system will make a best effort to return as close to as `max_tasks` as possible.
   *
   * The largest that `max_tasks` can be is 1000.
   *
   * The maximum total size of a lease tasks response is 32 MB. If the sum of all task sizes
   * requested reaches this limit, fewer tasks than requested are returned.
   * @return value or {@code null} for none
   */
  public java.lang.Integer getMaxTasks() {
    return maxTasks;
  }

  /**
   * The maximum number of tasks to lease.
   *
   * The system will make a best effort to return as close to as `max_tasks` as possible.
   *
   * The largest that `max_tasks` can be is 1000.
   *
   * The maximum total size of a lease tasks response is 32 MB. If the sum of all task sizes
   * requested reaches this limit, fewer tasks than requested are returned.
   * @param maxTasks maxTasks or {@code null} for none
   */
  public LeaseTasksRequest setMaxTasks(java.lang.Integer maxTasks) {
    this.maxTasks = maxTasks;
    return this;
  }

  /**
   * The response_view specifies which subset of the Task will be returned.
   *
   * By default response_view is BASIC; not all information is retrieved by default because some
   * data, such as payloads, might be desirable to return only when needed because of its large size
   * or because of the sensitivity of data that it contains.
   *
   * Authorization for FULL requires `cloudtasks.tasks.fullView` [Google
   * IAM](https://cloud.google.com/iam/) permission on the Task resource.
   * @return value or {@code null} for none
   */
  public java.lang.String getResponseView() {
    return responseView;
  }

  /**
   * The response_view specifies which subset of the Task will be returned.
   *
   * By default response_view is BASIC; not all information is retrieved by default because some
   * data, such as payloads, might be desirable to return only when needed because of its large size
   * or because of the sensitivity of data that it contains.
   *
   * Authorization for FULL requires `cloudtasks.tasks.fullView` [Google
   * IAM](https://cloud.google.com/iam/) permission on the Task resource.
   * @param responseView responseView or {@code null} for none
   */
  public LeaseTasksRequest setResponseView(java.lang.String responseView) {
    this.responseView = responseView;
    return this;
  }

  @Override
  public LeaseTasksRequest set(String fieldName, Object value) {
    return (LeaseTasksRequest) super.set(fieldName, value);
  }

  @Override
  public LeaseTasksRequest clone() {
    return (LeaseTasksRequest) super.clone();
  }

}
