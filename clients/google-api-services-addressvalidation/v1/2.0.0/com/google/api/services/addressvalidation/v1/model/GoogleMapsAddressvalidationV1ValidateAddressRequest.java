/*
 * Licensed under the Apache License, Version 2.0 (the "License"); you may not use this file except
 * in compliance with the License. You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software distributed under the License
 * is distributed on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express
 * or implied. See the License for the specific language governing permissions and limitations under
 * the License.
 */
/*
 * This code was generated by https://github.com/googleapis/google-api-java-client-services/
 * Modify at your own risk.
 */

package com.google.api.services.addressvalidation.v1.model;

/**
 * The request for validating an address.
 *
 * <p> This is the Java data model class that specifies how to parse/serialize into the JSON that is
 * transmitted over HTTP when working with the Address Validation API. For a detailed explanation
 * see:
 * <a href="https://developers.google.com/api-client-library/java/google-http-java-client/json">https://developers.google.com/api-client-library/java/google-http-java-client/json</a>
 * </p>
 *
 * @author Google, Inc.
 */
@SuppressWarnings("javadoc")
public final class GoogleMapsAddressvalidationV1ValidateAddressRequest extends com.google.api.client.json.GenericJson {

  /**
   * Required. The address being validated. Unformatted addresses should be submitted via
   * `address_lines`. The total length of the fields in this input must not exceed 280 characters.
   * Supported regions can be found [here](https://developers.google.com/maps/documentation/address-
   * validation/coverage). The language_code value in the input address is reserved for future uses
   * and is ignored today. The validated address result will be populated based on the preferred
   * language for the given address, as identified by the system. The Address Validation API ignores
   * the values in recipients and organization. Any values in those fields will be discarded and not
   * returned. Please do not set them.
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private GoogleTypePostalAddress address;

  /**
   * Enables USPS CASS compatible mode. This affects _only_ the
   * [google.maps.addressvalidation.v1.ValidationResult.usps_data] field of
   * [google.maps.addressvalidation.v1.ValidationResult]. Note: for USPS CASS enabled requests for
   * addresses in Puerto Rico, a [google.type.PostalAddress.region_code] of the `address` must be
   * provided as "PR", or an [google.type.PostalAddress.administrative_area] of the `address` must
   * be provided as "Puerto Rico" (case-insensitive) or "PR". It's recommended to use a
   * componentized `address`, or alternatively specify at least two
   * [google.type.PostalAddress.address_lines] where the first line contains the street number and
   * name and the second line contains the city, state, and zip code.
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private java.lang.Boolean enableUspsCass;

  /**
   * Optional. Preview: This feature is in Preview (pre-GA). Pre-GA products and features might have
   * limited support, and changes to pre-GA products and features might not be compatible with other
   * pre-GA versions. Pre-GA Offerings are covered by the [Google Maps Platform Service Specific
   * Terms](https://cloud.google.com/maps-platform/terms/maps-service-terms). For more information,
   * see the [launch stage descriptions](https://developers.google.com/maps/launch-stages). Enables
   * the Address Validation API to include additional information in the response.
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private GoogleMapsAddressvalidationV1LanguageOptions languageOptions;

  /**
   * This field must be empty for the first address validation request. If more requests are
   * necessary to fully validate a single address (for example if the changes the user makes after
   * the initial validation need to be re-validated), then each followup request must populate this
   * field with the response_id from the very first response in the validation sequence.
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private java.lang.String previousResponseId;

  /**
   * Optional. A string which identifies an Autocomplete session for billing purposes. Must be a URL
   * and filename safe base64 string with at most 36 ASCII characters in length. Otherwise an
   * INVALID_ARGUMENT error is returned. The session begins when the user makes an Autocomplete
   * query, and concludes when they select a place and a call to Place Details or Address Validation
   * is made. Each session can have multiple Autocomplete queries, followed by one Place Details or
   * Address Validation request. The credentials used for each request within a session must belong
   * to the same Google Cloud Console project. Once a session has concluded, the token is no longer
   * valid; your app must generate a fresh token for each session. If the `sessionToken` parameter
   * is omitted, or if you reuse a session token, the session is charged as if no session token was
   * provided (each request is billed separately). Note: Address Validation can only be used in
   * sessions with the Autocomplete (New) API, not the Autocomplete API. See
   * https://developers.google.com/maps/documentation/places/web-service/session-pricing for more
   * details.
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private java.lang.String sessionToken;

  /**
   * Required. The address being validated. Unformatted addresses should be submitted via
   * `address_lines`. The total length of the fields in this input must not exceed 280 characters.
   * Supported regions can be found [here](https://developers.google.com/maps/documentation/address-
   * validation/coverage). The language_code value in the input address is reserved for future uses
   * and is ignored today. The validated address result will be populated based on the preferred
   * language for the given address, as identified by the system. The Address Validation API ignores
   * the values in recipients and organization. Any values in those fields will be discarded and not
   * returned. Please do not set them.
   * @return value or {@code null} for none
   */
  public GoogleTypePostalAddress getAddress() {
    return address;
  }

  /**
   * Required. The address being validated. Unformatted addresses should be submitted via
   * `address_lines`. The total length of the fields in this input must not exceed 280 characters.
   * Supported regions can be found [here](https://developers.google.com/maps/documentation/address-
   * validation/coverage). The language_code value in the input address is reserved for future uses
   * and is ignored today. The validated address result will be populated based on the preferred
   * language for the given address, as identified by the system. The Address Validation API ignores
   * the values in recipients and organization. Any values in those fields will be discarded and not
   * returned. Please do not set them.
   * @param address address or {@code null} for none
   */
  public GoogleMapsAddressvalidationV1ValidateAddressRequest setAddress(GoogleTypePostalAddress address) {
    this.address = address;
    return this;
  }

  /**
   * Enables USPS CASS compatible mode. This affects _only_ the
   * [google.maps.addressvalidation.v1.ValidationResult.usps_data] field of
   * [google.maps.addressvalidation.v1.ValidationResult]. Note: for USPS CASS enabled requests for
   * addresses in Puerto Rico, a [google.type.PostalAddress.region_code] of the `address` must be
   * provided as "PR", or an [google.type.PostalAddress.administrative_area] of the `address` must
   * be provided as "Puerto Rico" (case-insensitive) or "PR". It's recommended to use a
   * componentized `address`, or alternatively specify at least two
   * [google.type.PostalAddress.address_lines] where the first line contains the street number and
   * name and the second line contains the city, state, and zip code.
   * @return value or {@code null} for none
   */
  public java.lang.Boolean getEnableUspsCass() {
    return enableUspsCass;
  }

  /**
   * Enables USPS CASS compatible mode. This affects _only_ the
   * [google.maps.addressvalidation.v1.ValidationResult.usps_data] field of
   * [google.maps.addressvalidation.v1.ValidationResult]. Note: for USPS CASS enabled requests for
   * addresses in Puerto Rico, a [google.type.PostalAddress.region_code] of the `address` must be
   * provided as "PR", or an [google.type.PostalAddress.administrative_area] of the `address` must
   * be provided as "Puerto Rico" (case-insensitive) or "PR". It's recommended to use a
   * componentized `address`, or alternatively specify at least two
   * [google.type.PostalAddress.address_lines] where the first line contains the street number and
   * name and the second line contains the city, state, and zip code.
   * @param enableUspsCass enableUspsCass or {@code null} for none
   */
  public GoogleMapsAddressvalidationV1ValidateAddressRequest setEnableUspsCass(java.lang.Boolean enableUspsCass) {
    this.enableUspsCass = enableUspsCass;
    return this;
  }

  /**
   * Optional. Preview: This feature is in Preview (pre-GA). Pre-GA products and features might have
   * limited support, and changes to pre-GA products and features might not be compatible with other
   * pre-GA versions. Pre-GA Offerings are covered by the [Google Maps Platform Service Specific
   * Terms](https://cloud.google.com/maps-platform/terms/maps-service-terms). For more information,
   * see the [launch stage descriptions](https://developers.google.com/maps/launch-stages). Enables
   * the Address Validation API to include additional information in the response.
   * @return value or {@code null} for none
   */
  public GoogleMapsAddressvalidationV1LanguageOptions getLanguageOptions() {
    return languageOptions;
  }

  /**
   * Optional. Preview: This feature is in Preview (pre-GA). Pre-GA products and features might have
   * limited support, and changes to pre-GA products and features might not be compatible with other
   * pre-GA versions. Pre-GA Offerings are covered by the [Google Maps Platform Service Specific
   * Terms](https://cloud.google.com/maps-platform/terms/maps-service-terms). For more information,
   * see the [launch stage descriptions](https://developers.google.com/maps/launch-stages). Enables
   * the Address Validation API to include additional information in the response.
   * @param languageOptions languageOptions or {@code null} for none
   */
  public GoogleMapsAddressvalidationV1ValidateAddressRequest setLanguageOptions(GoogleMapsAddressvalidationV1LanguageOptions languageOptions) {
    this.languageOptions = languageOptions;
    return this;
  }

  /**
   * This field must be empty for the first address validation request. If more requests are
   * necessary to fully validate a single address (for example if the changes the user makes after
   * the initial validation need to be re-validated), then each followup request must populate this
   * field with the response_id from the very first response in the validation sequence.
   * @return value or {@code null} for none
   */
  public java.lang.String getPreviousResponseId() {
    return previousResponseId;
  }

  /**
   * This field must be empty for the first address validation request. If more requests are
   * necessary to fully validate a single address (for example if the changes the user makes after
   * the initial validation need to be re-validated), then each followup request must populate this
   * field with the response_id from the very first response in the validation sequence.
   * @param previousResponseId previousResponseId or {@code null} for none
   */
  public GoogleMapsAddressvalidationV1ValidateAddressRequest setPreviousResponseId(java.lang.String previousResponseId) {
    this.previousResponseId = previousResponseId;
    return this;
  }

  /**
   * Optional. A string which identifies an Autocomplete session for billing purposes. Must be a URL
   * and filename safe base64 string with at most 36 ASCII characters in length. Otherwise an
   * INVALID_ARGUMENT error is returned. The session begins when the user makes an Autocomplete
   * query, and concludes when they select a place and a call to Place Details or Address Validation
   * is made. Each session can have multiple Autocomplete queries, followed by one Place Details or
   * Address Validation request. The credentials used for each request within a session must belong
   * to the same Google Cloud Console project. Once a session has concluded, the token is no longer
   * valid; your app must generate a fresh token for each session. If the `sessionToken` parameter
   * is omitted, or if you reuse a session token, the session is charged as if no session token was
   * provided (each request is billed separately). Note: Address Validation can only be used in
   * sessions with the Autocomplete (New) API, not the Autocomplete API. See
   * https://developers.google.com/maps/documentation/places/web-service/session-pricing for more
   * details.
   * @return value or {@code null} for none
   */
  public java.lang.String getSessionToken() {
    return sessionToken;
  }

  /**
   * Optional. A string which identifies an Autocomplete session for billing purposes. Must be a URL
   * and filename safe base64 string with at most 36 ASCII characters in length. Otherwise an
   * INVALID_ARGUMENT error is returned. The session begins when the user makes an Autocomplete
   * query, and concludes when they select a place and a call to Place Details or Address Validation
   * is made. Each session can have multiple Autocomplete queries, followed by one Place Details or
   * Address Validation request. The credentials used for each request within a session must belong
   * to the same Google Cloud Console project. Once a session has concluded, the token is no longer
   * valid; your app must generate a fresh token for each session. If the `sessionToken` parameter
   * is omitted, or if you reuse a session token, the session is charged as if no session token was
   * provided (each request is billed separately). Note: Address Validation can only be used in
   * sessions with the Autocomplete (New) API, not the Autocomplete API. See
   * https://developers.google.com/maps/documentation/places/web-service/session-pricing for more
   * details.
   * @param sessionToken sessionToken or {@code null} for none
   */
  public GoogleMapsAddressvalidationV1ValidateAddressRequest setSessionToken(java.lang.String sessionToken) {
    this.sessionToken = sessionToken;
    return this;
  }

  @Override
  public GoogleMapsAddressvalidationV1ValidateAddressRequest set(String fieldName, Object value) {
    return (GoogleMapsAddressvalidationV1ValidateAddressRequest) super.set(fieldName, value);
  }

  @Override
  public GoogleMapsAddressvalidationV1ValidateAddressRequest clone() {
    return (GoogleMapsAddressvalidationV1ValidateAddressRequest) super.clone();
  }

}
