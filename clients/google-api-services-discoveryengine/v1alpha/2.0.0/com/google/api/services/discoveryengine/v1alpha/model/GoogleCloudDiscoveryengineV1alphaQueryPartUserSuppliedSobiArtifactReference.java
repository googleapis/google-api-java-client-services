/*
 * Licensed under the Apache License, Version 2.0 (the "License"); you may not use this file except
 * in compliance with the License. You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software distributed under the License
 * is distributed on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express
 * or implied. See the License for the specific language governing permissions and limitations under
 * the License.
 */
/*
 * This code was generated by https://github.com/googleapis/google-api-java-client-services/
 * Modify at your own risk.
 */

package com.google.api.services.discoveryengine.v1alpha.model;

/**
 * Reference to a user-supplied file attached to a long-running-agent (Sobi) task. Rendered by the
 * client as an attachment chip on the user turn's bubble. The file itself is stored on the task's
 * `AgentArtifact` list; the client resolves full metadata (source URI, size, ...) for download or
 * preview by calling `GetTask` on the Sobi Task API and matching `AgentArtifact.name` against
 * `file_name`. Only `file_name` and `mime_type` appear on this wire deliberately: the backing
 * `gs://` or Drive URI is not exposed to conversation-history readers. Same variant is used for
 * both byte-uploaded and Drive-picked files -- "user attached a file" is one concept regardless of
 * source.
 *
 * <p> This is the Java data model class that specifies how to parse/serialize into the JSON that is
 * transmitted over HTTP when working with the Discovery Engine API. For a detailed explanation see:
 * <a href="https://developers.google.com/api-client-library/java/google-http-java-client/json">https://developers.google.com/api-client-library/java/google-http-java-client/json</a>
 * </p>
 *
 * @author Google, Inc.
 */
@SuppressWarnings("javadoc")
public final class GoogleCloudDiscoveryengineV1alphaQueryPartUserSuppliedSobiArtifactReference extends com.google.api.client.json.GenericJson {

  /**
   * Required. Client-supplied filename. Unique per task within the user-supplied artifact set (UI
   * dedupes before upload). Doubles as the artifact identifier used to look up the artifact on
   * `Task.artifacts`. The `_name` suffix is deliberate and the AIP-122 lint is suppressed: this
   * field is not a resource name (which is what AIP-122 reserves the bare `name` for). It is the
   * literal filename, and it serves as the join key against `AgentArtifact.name` on the backing
   * Sobi task -- a structural role that `title` or `display_name` would not communicate to a client
   * picking what to send. The internal mirror (`cloud/ml/discoveryengine/schema/assistant.proto`)
   * uses the same field name so the round-trip converter stays name-for-name.
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private java.lang.String fileName;

  /**
   * Optional. IANA MIME type. Used for icon/preview rendering.
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private java.lang.String mimeType;

  /**
   * Required. Client-supplied filename. Unique per task within the user-supplied artifact set (UI
   * dedupes before upload). Doubles as the artifact identifier used to look up the artifact on
   * `Task.artifacts`. The `_name` suffix is deliberate and the AIP-122 lint is suppressed: this
   * field is not a resource name (which is what AIP-122 reserves the bare `name` for). It is the
   * literal filename, and it serves as the join key against `AgentArtifact.name` on the backing
   * Sobi task -- a structural role that `title` or `display_name` would not communicate to a client
   * picking what to send. The internal mirror (`cloud/ml/discoveryengine/schema/assistant.proto`)
   * uses the same field name so the round-trip converter stays name-for-name.
   * @return value or {@code null} for none
   */
  public java.lang.String getFileName() {
    return fileName;
  }

  /**
   * Required. Client-supplied filename. Unique per task within the user-supplied artifact set (UI
   * dedupes before upload). Doubles as the artifact identifier used to look up the artifact on
   * `Task.artifacts`. The `_name` suffix is deliberate and the AIP-122 lint is suppressed: this
   * field is not a resource name (which is what AIP-122 reserves the bare `name` for). It is the
   * literal filename, and it serves as the join key against `AgentArtifact.name` on the backing
   * Sobi task -- a structural role that `title` or `display_name` would not communicate to a client
   * picking what to send. The internal mirror (`cloud/ml/discoveryengine/schema/assistant.proto`)
   * uses the same field name so the round-trip converter stays name-for-name.
   * @param fileName fileName or {@code null} for none
   */
  public GoogleCloudDiscoveryengineV1alphaQueryPartUserSuppliedSobiArtifactReference setFileName(java.lang.String fileName) {
    this.fileName = fileName;
    return this;
  }

  /**
   * Optional. IANA MIME type. Used for icon/preview rendering.
   * @return value or {@code null} for none
   */
  public java.lang.String getMimeType() {
    return mimeType;
  }

  /**
   * Optional. IANA MIME type. Used for icon/preview rendering.
   * @param mimeType mimeType or {@code null} for none
   */
  public GoogleCloudDiscoveryengineV1alphaQueryPartUserSuppliedSobiArtifactReference setMimeType(java.lang.String mimeType) {
    this.mimeType = mimeType;
    return this;
  }

  @Override
  public GoogleCloudDiscoveryengineV1alphaQueryPartUserSuppliedSobiArtifactReference set(String fieldName, Object value) {
    return (GoogleCloudDiscoveryengineV1alphaQueryPartUserSuppliedSobiArtifactReference) super.set(fieldName, value);
  }

  @Override
  public GoogleCloudDiscoveryengineV1alphaQueryPartUserSuppliedSobiArtifactReference clone() {
    return (GoogleCloudDiscoveryengineV1alphaQueryPartUserSuppliedSobiArtifactReference) super.clone();
  }

}
