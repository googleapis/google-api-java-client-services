/*
 * Licensed under the Apache License, Version 2.0 (the "License"); you may not use this file except
 * in compliance with the License. You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software distributed under the License
 * is distributed on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express
 * or implied. See the License for the specific language governing permissions and limitations under
 * the License.
 */
/*
 * This code was generated by https://github.com/googleapis/google-api-java-client-services/
 * Modify at your own risk.
 */

package com.google.api.services.compute.model;

/**
 * Message containing CachePolicy configuration for URL Map's Route Action.
 *
 * <p> This is the Java data model class that specifies how to parse/serialize into the JSON that is
 * transmitted over HTTP when working with the Compute Engine API. For a detailed explanation see:
 * <a href="https://developers.google.com/api-client-library/java/google-http-java-client/json">https://developers.google.com/api-client-library/java/google-http-java-client/json</a>
 * </p>
 *
 * @author Google, Inc.
 */
@SuppressWarnings("javadoc")
public final class CachePolicy extends com.google.api.client.json.GenericJson {

  /**
   * Bypass the cache when the specified request headers are matched by name, e.g. Pragma or
   * Authorization headers. Values are case-insensitive. Up to 5 header names can be specified. The
   * cache is bypassed for all `cacheMode` values.
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private java.util.List<java.lang.String> cacheBypassRequestHeaderNames;

  /**
   * The cache key configuration. If not specified, the default behavior depends on the backend
   * type: for Backend Services, the complete request URI is used; for Backend Buckets, the request
   * URI is used without the protocol or host, and only query parameters known to Cloud Storage are
   * included.
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private CachePolicyCacheKeyPolicy cacheKeyPolicy;

  /**
   * Specifies the cache setting for all responses from this route. If not specified, Cloud CDN uses
   * `CACHE_ALL_STATIC` mode.
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private java.lang.String cacheMode;

  /**
   * Specifies a separate client (e.g. browser client) maximum TTL for cached content. This is used
   * to clamp the max-age (or Expires) value sent to the client. With `FORCE_CACHE_ALL`, the lesser
   * of `clientTtl` and `defaultTtl` is used for the response max-age directive, along with a
   * "public" directive. For cacheable content in `CACHE_ALL_STATIC` mode, `clientTtl` clamps the
   * max-age from the origin (if specified), or else sets the response max-age directive to the
   * lesser of the `clientTtl` and `defaultTtl`, and also ensures a "public" cache-control directive
   * is present. The maximum allowed value is 31,622,400s (1 year). If not specified, Cloud CDN uses
   * 3600s (1 hour) for `CACHE_ALL_STATIC` mode. Cannot exceed `maxTtl`. Cannot be specified when
   * `cacheMode` is `USE_ORIGIN_HEADERS`.
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private Duration clientTtl;

  /**
   * Specifies the default TTL for cached content for responses that do not have an existing valid
   * TTL (max-age or s-maxage). Setting a TTL of "0" means "always revalidate". The value of
   * `defaultTtl` cannot be set to a value greater than that of `maxTtl`. When the `cacheMode` is
   * set to `FORCE_CACHE_ALL`, the `defaultTtl` will overwrite the TTL set in all responses. The
   * maximum allowed value is 31,622,400s (1 year). Infrequently accessed objects may be evicted
   * from the cache before the defined TTL. If not specified, Cloud CDN uses 3600s (1 hour) for
   * `CACHE_ALL_STATIC` and `FORCE_CACHE_ALL` modes. Cannot be specified when `cacheMode` is
   * `USE_ORIGIN_HEADERS`.
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private Duration defaultTtl;

  /**
   * Specifies the maximum allowed TTL for cached content. Cache directives that attempt to set a
   * max-age or s-maxage higher than this, or an Expires header more than `maxTtl` seconds in the
   * future will be capped at the value of `maxTtl`, as if it were the value of an s-maxage Cache-
   * Control directive. Headers sent to the client will not be modified. Setting a TTL of "0" means
   * "always revalidate". The maximum allowed value is 31,622,400s (1 year). Infrequently accessed
   * objects may be evicted from the cache before the defined TTL. If not specified, Cloud CDN uses
   * 86400s (1 day) for `CACHE_ALL_STATIC` mode. Can be specified only for `CACHE_ALL_STATIC` cache
   * mode.
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private Duration maxTtl;

  /**
   * Negative caching allows per-status code TTLs to be set, in order to apply fine-grained caching
   * for common errors or redirects. This can reduce the load on your origin and improve end-user
   * experience by reducing response latency. When the `cacheMode` is set to `CACHE_ALL_STATIC` or
   * `USE_ORIGIN_HEADERS`, negative caching applies to responses with the specified response code
   * that lack any Cache-Control, Expires, or Pragma: no-cache directives. When the `cacheMode` is
   * set to `FORCE_CACHE_ALL`, negative caching applies to all responses with the specified response
   * code, and overrides any caching headers. By default, Cloud CDN applies the following TTLs to
   * these HTTP status codes:
   *
   * * 300 (Multiple Choice), 301, 308 (Permanent Redirects): 10m * 404 (Not Found), 410 (Gone), 451
   * (Unavailable For Legal Reasons): 120s * 405 (Method Not Found), 501 (Not Implemented): 60s
   *
   * These defaults can be overridden in `negativeCachingPolicy`. If not specified, Cloud CDN
   * applies negative caching by default.
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private java.lang.Boolean negativeCaching;

  /**
   * Sets a cache TTL for the specified HTTP status code. `negativeCaching` must be enabled to
   * configure `negativeCachingPolicy`. Omitting the policy and leaving `negativeCaching` enabled
   * will use Cloud CDN's default cache TTLs. Note that when specifying an explicit
   * `negativeCachingPolicy`, you should take care to specify a cache TTL for all response codes
   * that you wish to cache. Cloud CDN will not apply any default negative caching when a policy
   * exists.
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private java.util.List<CachePolicyNegativeCachingPolicy> negativeCachingPolicy;

  /**
   * If true then Cloud CDN will combine multiple concurrent cache fill requests into a small number
   * of requests to the origin. If not specified, Cloud CDN applies request coalescing by default.
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private java.lang.Boolean requestCoalescing;

  /**
   * Serve existing content from the cache (if available) when revalidating content with the origin,
   * or when an error is encountered when refreshing the cache. This setting defines the default
   * "max-stale" duration for any cached responses that do not specify a max-stale directive. Stale
   * responses that exceed the TTL configured here will not be served. The default limit (max-stale)
   * is 86400s (1 day), which will allow stale content to be served up to this limit beyond the max-
   * age (or s-maxage) of a cached response. The maximum allowed value is 604800 (1 week). Set this
   * to zero (0) to disable serve-while-stale.
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private Duration serveWhileStale;

  /**
   * Bypass the cache when the specified request headers are matched by name, e.g. Pragma or
   * Authorization headers. Values are case-insensitive. Up to 5 header names can be specified. The
   * cache is bypassed for all `cacheMode` values.
   * @return value or {@code null} for none
   */
  public java.util.List<java.lang.String> getCacheBypassRequestHeaderNames() {
    return cacheBypassRequestHeaderNames;
  }

  /**
   * Bypass the cache when the specified request headers are matched by name, e.g. Pragma or
   * Authorization headers. Values are case-insensitive. Up to 5 header names can be specified. The
   * cache is bypassed for all `cacheMode` values.
   * @param cacheBypassRequestHeaderNames cacheBypassRequestHeaderNames or {@code null} for none
   */
  public CachePolicy setCacheBypassRequestHeaderNames(java.util.List<java.lang.String> cacheBypassRequestHeaderNames) {
    this.cacheBypassRequestHeaderNames = cacheBypassRequestHeaderNames;
    return this;
  }

  /**
   * The cache key configuration. If not specified, the default behavior depends on the backend
   * type: for Backend Services, the complete request URI is used; for Backend Buckets, the request
   * URI is used without the protocol or host, and only query parameters known to Cloud Storage are
   * included.
   * @return value or {@code null} for none
   */
  public CachePolicyCacheKeyPolicy getCacheKeyPolicy() {
    return cacheKeyPolicy;
  }

  /**
   * The cache key configuration. If not specified, the default behavior depends on the backend
   * type: for Backend Services, the complete request URI is used; for Backend Buckets, the request
   * URI is used without the protocol or host, and only query parameters known to Cloud Storage are
   * included.
   * @param cacheKeyPolicy cacheKeyPolicy or {@code null} for none
   */
  public CachePolicy setCacheKeyPolicy(CachePolicyCacheKeyPolicy cacheKeyPolicy) {
    this.cacheKeyPolicy = cacheKeyPolicy;
    return this;
  }

  /**
   * Specifies the cache setting for all responses from this route. If not specified, Cloud CDN uses
   * `CACHE_ALL_STATIC` mode.
   * @return value or {@code null} for none
   */
  public java.lang.String getCacheMode() {
    return cacheMode;
  }

  /**
   * Specifies the cache setting for all responses from this route. If not specified, Cloud CDN uses
   * `CACHE_ALL_STATIC` mode.
   * @param cacheMode cacheMode or {@code null} for none
   */
  public CachePolicy setCacheMode(java.lang.String cacheMode) {
    this.cacheMode = cacheMode;
    return this;
  }

  /**
   * Specifies a separate client (e.g. browser client) maximum TTL for cached content. This is used
   * to clamp the max-age (or Expires) value sent to the client. With `FORCE_CACHE_ALL`, the lesser
   * of `clientTtl` and `defaultTtl` is used for the response max-age directive, along with a
   * "public" directive. For cacheable content in `CACHE_ALL_STATIC` mode, `clientTtl` clamps the
   * max-age from the origin (if specified), or else sets the response max-age directive to the
   * lesser of the `clientTtl` and `defaultTtl`, and also ensures a "public" cache-control directive
   * is present. The maximum allowed value is 31,622,400s (1 year). If not specified, Cloud CDN uses
   * 3600s (1 hour) for `CACHE_ALL_STATIC` mode. Cannot exceed `maxTtl`. Cannot be specified when
   * `cacheMode` is `USE_ORIGIN_HEADERS`.
   * @return value or {@code null} for none
   */
  public Duration getClientTtl() {
    return clientTtl;
  }

  /**
   * Specifies a separate client (e.g. browser client) maximum TTL for cached content. This is used
   * to clamp the max-age (or Expires) value sent to the client. With `FORCE_CACHE_ALL`, the lesser
   * of `clientTtl` and `defaultTtl` is used for the response max-age directive, along with a
   * "public" directive. For cacheable content in `CACHE_ALL_STATIC` mode, `clientTtl` clamps the
   * max-age from the origin (if specified), or else sets the response max-age directive to the
   * lesser of the `clientTtl` and `defaultTtl`, and also ensures a "public" cache-control directive
   * is present. The maximum allowed value is 31,622,400s (1 year). If not specified, Cloud CDN uses
   * 3600s (1 hour) for `CACHE_ALL_STATIC` mode. Cannot exceed `maxTtl`. Cannot be specified when
   * `cacheMode` is `USE_ORIGIN_HEADERS`.
   * @param clientTtl clientTtl or {@code null} for none
   */
  public CachePolicy setClientTtl(Duration clientTtl) {
    this.clientTtl = clientTtl;
    return this;
  }

  /**
   * Specifies the default TTL for cached content for responses that do not have an existing valid
   * TTL (max-age or s-maxage). Setting a TTL of "0" means "always revalidate". The value of
   * `defaultTtl` cannot be set to a value greater than that of `maxTtl`. When the `cacheMode` is
   * set to `FORCE_CACHE_ALL`, the `defaultTtl` will overwrite the TTL set in all responses. The
   * maximum allowed value is 31,622,400s (1 year). Infrequently accessed objects may be evicted
   * from the cache before the defined TTL. If not specified, Cloud CDN uses 3600s (1 hour) for
   * `CACHE_ALL_STATIC` and `FORCE_CACHE_ALL` modes. Cannot be specified when `cacheMode` is
   * `USE_ORIGIN_HEADERS`.
   * @return value or {@code null} for none
   */
  public Duration getDefaultTtl() {
    return defaultTtl;
  }

  /**
   * Specifies the default TTL for cached content for responses that do not have an existing valid
   * TTL (max-age or s-maxage). Setting a TTL of "0" means "always revalidate". The value of
   * `defaultTtl` cannot be set to a value greater than that of `maxTtl`. When the `cacheMode` is
   * set to `FORCE_CACHE_ALL`, the `defaultTtl` will overwrite the TTL set in all responses. The
   * maximum allowed value is 31,622,400s (1 year). Infrequently accessed objects may be evicted
   * from the cache before the defined TTL. If not specified, Cloud CDN uses 3600s (1 hour) for
   * `CACHE_ALL_STATIC` and `FORCE_CACHE_ALL` modes. Cannot be specified when `cacheMode` is
   * `USE_ORIGIN_HEADERS`.
   * @param defaultTtl defaultTtl or {@code null} for none
   */
  public CachePolicy setDefaultTtl(Duration defaultTtl) {
    this.defaultTtl = defaultTtl;
    return this;
  }

  /**
   * Specifies the maximum allowed TTL for cached content. Cache directives that attempt to set a
   * max-age or s-maxage higher than this, or an Expires header more than `maxTtl` seconds in the
   * future will be capped at the value of `maxTtl`, as if it were the value of an s-maxage Cache-
   * Control directive. Headers sent to the client will not be modified. Setting a TTL of "0" means
   * "always revalidate". The maximum allowed value is 31,622,400s (1 year). Infrequently accessed
   * objects may be evicted from the cache before the defined TTL. If not specified, Cloud CDN uses
   * 86400s (1 day) for `CACHE_ALL_STATIC` mode. Can be specified only for `CACHE_ALL_STATIC` cache
   * mode.
   * @return value or {@code null} for none
   */
  public Duration getMaxTtl() {
    return maxTtl;
  }

  /**
   * Specifies the maximum allowed TTL for cached content. Cache directives that attempt to set a
   * max-age or s-maxage higher than this, or an Expires header more than `maxTtl` seconds in the
   * future will be capped at the value of `maxTtl`, as if it were the value of an s-maxage Cache-
   * Control directive. Headers sent to the client will not be modified. Setting a TTL of "0" means
   * "always revalidate". The maximum allowed value is 31,622,400s (1 year). Infrequently accessed
   * objects may be evicted from the cache before the defined TTL. If not specified, Cloud CDN uses
   * 86400s (1 day) for `CACHE_ALL_STATIC` mode. Can be specified only for `CACHE_ALL_STATIC` cache
   * mode.
   * @param maxTtl maxTtl or {@code null} for none
   */
  public CachePolicy setMaxTtl(Duration maxTtl) {
    this.maxTtl = maxTtl;
    return this;
  }

  /**
   * Negative caching allows per-status code TTLs to be set, in order to apply fine-grained caching
   * for common errors or redirects. This can reduce the load on your origin and improve end-user
   * experience by reducing response latency. When the `cacheMode` is set to `CACHE_ALL_STATIC` or
   * `USE_ORIGIN_HEADERS`, negative caching applies to responses with the specified response code
   * that lack any Cache-Control, Expires, or Pragma: no-cache directives. When the `cacheMode` is
   * set to `FORCE_CACHE_ALL`, negative caching applies to all responses with the specified response
   * code, and overrides any caching headers. By default, Cloud CDN applies the following TTLs to
   * these HTTP status codes:
   *
   * * 300 (Multiple Choice), 301, 308 (Permanent Redirects): 10m * 404 (Not Found), 410 (Gone), 451
   * (Unavailable For Legal Reasons): 120s * 405 (Method Not Found), 501 (Not Implemented): 60s
   *
   * These defaults can be overridden in `negativeCachingPolicy`. If not specified, Cloud CDN
   * applies negative caching by default.
   * @return value or {@code null} for none
   */
  public java.lang.Boolean getNegativeCaching() {
    return negativeCaching;
  }

  /**
   * Negative caching allows per-status code TTLs to be set, in order to apply fine-grained caching
   * for common errors or redirects. This can reduce the load on your origin and improve end-user
   * experience by reducing response latency. When the `cacheMode` is set to `CACHE_ALL_STATIC` or
   * `USE_ORIGIN_HEADERS`, negative caching applies to responses with the specified response code
   * that lack any Cache-Control, Expires, or Pragma: no-cache directives. When the `cacheMode` is
   * set to `FORCE_CACHE_ALL`, negative caching applies to all responses with the specified response
   * code, and overrides any caching headers. By default, Cloud CDN applies the following TTLs to
   * these HTTP status codes:
   *
   * * 300 (Multiple Choice), 301, 308 (Permanent Redirects): 10m * 404 (Not Found), 410 (Gone), 451
   * (Unavailable For Legal Reasons): 120s * 405 (Method Not Found), 501 (Not Implemented): 60s
   *
   * These defaults can be overridden in `negativeCachingPolicy`. If not specified, Cloud CDN
   * applies negative caching by default.
   * @param negativeCaching negativeCaching or {@code null} for none
   */
  public CachePolicy setNegativeCaching(java.lang.Boolean negativeCaching) {
    this.negativeCaching = negativeCaching;
    return this;
  }

  /**
   * Sets a cache TTL for the specified HTTP status code. `negativeCaching` must be enabled to
   * configure `negativeCachingPolicy`. Omitting the policy and leaving `negativeCaching` enabled
   * will use Cloud CDN's default cache TTLs. Note that when specifying an explicit
   * `negativeCachingPolicy`, you should take care to specify a cache TTL for all response codes
   * that you wish to cache. Cloud CDN will not apply any default negative caching when a policy
   * exists.
   * @return value or {@code null} for none
   */
  public java.util.List<CachePolicyNegativeCachingPolicy> getNegativeCachingPolicy() {
    return negativeCachingPolicy;
  }

  /**
   * Sets a cache TTL for the specified HTTP status code. `negativeCaching` must be enabled to
   * configure `negativeCachingPolicy`. Omitting the policy and leaving `negativeCaching` enabled
   * will use Cloud CDN's default cache TTLs. Note that when specifying an explicit
   * `negativeCachingPolicy`, you should take care to specify a cache TTL for all response codes
   * that you wish to cache. Cloud CDN will not apply any default negative caching when a policy
   * exists.
   * @param negativeCachingPolicy negativeCachingPolicy or {@code null} for none
   */
  public CachePolicy setNegativeCachingPolicy(java.util.List<CachePolicyNegativeCachingPolicy> negativeCachingPolicy) {
    this.negativeCachingPolicy = negativeCachingPolicy;
    return this;
  }

  /**
   * If true then Cloud CDN will combine multiple concurrent cache fill requests into a small number
   * of requests to the origin. If not specified, Cloud CDN applies request coalescing by default.
   * @return value or {@code null} for none
   */
  public java.lang.Boolean getRequestCoalescing() {
    return requestCoalescing;
  }

  /**
   * If true then Cloud CDN will combine multiple concurrent cache fill requests into a small number
   * of requests to the origin. If not specified, Cloud CDN applies request coalescing by default.
   * @param requestCoalescing requestCoalescing or {@code null} for none
   */
  public CachePolicy setRequestCoalescing(java.lang.Boolean requestCoalescing) {
    this.requestCoalescing = requestCoalescing;
    return this;
  }

  /**
   * Serve existing content from the cache (if available) when revalidating content with the origin,
   * or when an error is encountered when refreshing the cache. This setting defines the default
   * "max-stale" duration for any cached responses that do not specify a max-stale directive. Stale
   * responses that exceed the TTL configured here will not be served. The default limit (max-stale)
   * is 86400s (1 day), which will allow stale content to be served up to this limit beyond the max-
   * age (or s-maxage) of a cached response. The maximum allowed value is 604800 (1 week). Set this
   * to zero (0) to disable serve-while-stale.
   * @return value or {@code null} for none
   */
  public Duration getServeWhileStale() {
    return serveWhileStale;
  }

  /**
   * Serve existing content from the cache (if available) when revalidating content with the origin,
   * or when an error is encountered when refreshing the cache. This setting defines the default
   * "max-stale" duration for any cached responses that do not specify a max-stale directive. Stale
   * responses that exceed the TTL configured here will not be served. The default limit (max-stale)
   * is 86400s (1 day), which will allow stale content to be served up to this limit beyond the max-
   * age (or s-maxage) of a cached response. The maximum allowed value is 604800 (1 week). Set this
   * to zero (0) to disable serve-while-stale.
   * @param serveWhileStale serveWhileStale or {@code null} for none
   */
  public CachePolicy setServeWhileStale(Duration serveWhileStale) {
    this.serveWhileStale = serveWhileStale;
    return this;
  }

  @Override
  public CachePolicy set(String fieldName, Object value) {
    return (CachePolicy) super.set(fieldName, value);
  }

  @Override
  public CachePolicy clone() {
    return (CachePolicy) super.clone();
  }

}
