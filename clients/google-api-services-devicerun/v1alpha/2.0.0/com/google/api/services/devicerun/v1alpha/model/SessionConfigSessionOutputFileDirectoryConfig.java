/*
 * Licensed under the Apache License, Version 2.0 (the "License"); you may not use this file except
 * in compliance with the License. You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software distributed under the License
 * is distributed on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express
 * or implied. See the License for the specific language governing permissions and limitations under
 * the License.
 */
/*
 * This code was generated by https://github.com/googleapis/google-api-java-client-services/
 * Modify at your own risk.
 */

package com.google.api.services.devicerun.v1alpha.model;

/**
 * Config to control session output file directory.
 *
 * <p> This is the Java data model class that specifies how to parse/serialize into the JSON that is
 * transmitted over HTTP when working with the Device Run API. For a detailed explanation see:
 * <a href="https://developers.google.com/api-client-library/java/google-http-java-client/json">https://developers.google.com/api-client-library/java/google-http-java-client/json</a>
 * </p>
 *
 * @author Google, Inc.
 */
@SuppressWarnings("javadoc")
public final class SessionConfigSessionOutputFileDirectoryConfig extends com.google.api.client.json.GenericJson {

  /**
   * Optional. Whether to write output files directly under the output directory instead of nesting
   * them under service-generated subdirectories. By default (`false`), output files are stored
   * under `/`. When `true`, the session ID subdirectory is never appended, and the job display name
   * subdirectory is appended only when the session has more than one job. Output files are
   * therefore stored under: - `//` for a single-job session. - `` for a multi-job session. Set this
   * to `true` when the output directory is already unique per session (for example, when a CI
   * system generates it), to avoid redundant nesting.
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private java.lang.Boolean flatDirectoryStructure;

  /**
   * The Google Cloud Storage path of the output directory (e.g. `gs://my-bucket/output`). The
   * bucket must exist. If the bucket is located in another project or uses fine-grained access
   * controls, ensure the Device Run Service Agent of the project (`service-@gcp-sa-
   * devicerun.iam.gserviceaccount.com`) is granted access to the bucket (such as
   * `roles/storage.objectUser`).
   * The value may be {@code null}.
   */
  @com.google.api.client.util.Key
  private GcsPath gcsOutputDirectory;

  /**
   * Optional. Whether to write output files directly under the output directory instead of nesting
   * them under service-generated subdirectories. By default (`false`), output files are stored
   * under `/`. When `true`, the session ID subdirectory is never appended, and the job display name
   * subdirectory is appended only when the session has more than one job. Output files are
   * therefore stored under: - `//` for a single-job session. - `` for a multi-job session. Set this
   * to `true` when the output directory is already unique per session (for example, when a CI
   * system generates it), to avoid redundant nesting.
   * @return value or {@code null} for none
   */
  public java.lang.Boolean getFlatDirectoryStructure() {
    return flatDirectoryStructure;
  }

  /**
   * Optional. Whether to write output files directly under the output directory instead of nesting
   * them under service-generated subdirectories. By default (`false`), output files are stored
   * under `/`. When `true`, the session ID subdirectory is never appended, and the job display name
   * subdirectory is appended only when the session has more than one job. Output files are
   * therefore stored under: - `//` for a single-job session. - `` for a multi-job session. Set this
   * to `true` when the output directory is already unique per session (for example, when a CI
   * system generates it), to avoid redundant nesting.
   * @param flatDirectoryStructure flatDirectoryStructure or {@code null} for none
   */
  public SessionConfigSessionOutputFileDirectoryConfig setFlatDirectoryStructure(java.lang.Boolean flatDirectoryStructure) {
    this.flatDirectoryStructure = flatDirectoryStructure;
    return this;
  }

  /**
   * The Google Cloud Storage path of the output directory (e.g. `gs://my-bucket/output`). The
   * bucket must exist. If the bucket is located in another project or uses fine-grained access
   * controls, ensure the Device Run Service Agent of the project (`service-@gcp-sa-
   * devicerun.iam.gserviceaccount.com`) is granted access to the bucket (such as
   * `roles/storage.objectUser`).
   * @return value or {@code null} for none
   */
  public GcsPath getGcsOutputDirectory() {
    return gcsOutputDirectory;
  }

  /**
   * The Google Cloud Storage path of the output directory (e.g. `gs://my-bucket/output`). The
   * bucket must exist. If the bucket is located in another project or uses fine-grained access
   * controls, ensure the Device Run Service Agent of the project (`service-@gcp-sa-
   * devicerun.iam.gserviceaccount.com`) is granted access to the bucket (such as
   * `roles/storage.objectUser`).
   * @param gcsOutputDirectory gcsOutputDirectory or {@code null} for none
   */
  public SessionConfigSessionOutputFileDirectoryConfig setGcsOutputDirectory(GcsPath gcsOutputDirectory) {
    this.gcsOutputDirectory = gcsOutputDirectory;
    return this;
  }

  @Override
  public SessionConfigSessionOutputFileDirectoryConfig set(String fieldName, Object value) {
    return (SessionConfigSessionOutputFileDirectoryConfig) super.set(fieldName, value);
  }

  @Override
  public SessionConfigSessionOutputFileDirectoryConfig clone() {
    return (SessionConfigSessionOutputFileDirectoryConfig) super.clone();
  }

}
